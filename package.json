{
  "name": "initize",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "chokidar build.js -c 'node build' --initial",
    "build": "node build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ramda": "^0.23.0",
    "chokidar-cli": "^1.2.0",
    "mdn-data": "^1.0.0"
  }
}
